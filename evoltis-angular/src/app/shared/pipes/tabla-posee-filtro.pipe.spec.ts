import { TablaPoseeFiltroPipe } from './tabla-posee-filtro.pipe';

describe('TablaPoseeFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new TablaPoseeFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
